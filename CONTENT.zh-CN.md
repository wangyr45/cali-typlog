# 内容维护指南

安装后，文章和基础资料继续在 Typlog 后台维护。发布新文章后，首页自动显示最近 10 篇，不需要修改模板或重新打包。这里的「作品」包括后台最近发布的文章和播客，不包含独立页面。

## 日常内容在哪里修改

| 内容 | Typlog 中的入口 | 主题中的位置 |
| --- | --- | --- |
| 文章、标题、摘要、封面、发布时间 | 文章编辑器 | 首页、归档和详情页 |
| 播客、音频及节目说明 | 播客编辑器 | 作品列表和播放器 |
| 关于页等独立内容 | 页面编辑器，发布后把地址加入导航 | 独立页面 |
| 站名、简短介绍 | 站点基础设置 | 首页介绍区 |
| 底栏头像 | 颜色与标志设置中的 Logo | 底栏彩色圆形头像 |
| 顶部头像 | 额外配置中的 `portrait` | 默认使用这次提供的新头像，独立于底栏 |
| 主要链接的名称、地址、顺序 | 设置 → 链接与社交 → 主要链接 | 首页前三个入口、完整菜单和页脚；关于入口也会出现在底栏 |
| 次要链接的名称、地址、顺序 | 设置 → 链接与社交 → 次要链接 | 底栏自动新增按钮，同时显示在完整菜单和页脚 |
| 社交链接 | 设置 → 链接与社交 | 页脚 |
| 分类内容 | 给文章添加标签，再把标签页地址加入导航 | 标签列表 |
| 搜索、订阅、评论 | 对应的功能设置 | 由 Typlog 原生组件处理 |

主题不会把预览中的示例文章写入你的后台。文章正文、权限、评论与订阅数据始终由 Typlog 管理。

## 底栏按钮

底栏保留首页、文章归档、项目和已配置的关于入口，之后按后台「次要链接」的顺序显示按钮。主要链接中的「棱镜通讯」「隐说」不会自动成为底栏按钮，首页入口和完整菜单仍可访问它们。

在「次要链接」中填写名称和地址，点击加号，再点击该分区的「更新」。新增、改名、调序、删除和新标签页设置都会随主题渲染同步，不需要编辑代码，也不需要 PRO 额外配置。链接较多时底栏可以横向滑动；底部联系区域也会保留对应入口。

微信公众号主页链接（`mp.weixin.qq.com`，包括带查询参数的主页地址）显示微信图标。只匹配官方域名，普通 QQ 链接和相似域名不会被误认。

底栏按链接域名识别知乎、小红书、Twitter/X、B站、YouTube、Telegram、GitHub、微博和 Instagram，包括常用短链接域名。使用主题内置图标，无需加载第三方图标服务；其他网站显示通用链接图标。名称仍来自后台，并提供悬停提示与无障碍标签。

关于入口从主要链接中识别「关于」「关于我」「关于我们」「About」「About me」「About us」，或站内 `/me`、`/about` 地址。沿用已配置的地址；没有对应条目就隐藏该按钮，不会生成一个尚未发布的关于页。

## 文章左侧配图

在文章编辑器右侧发布设置选择「编辑封面」，上传或替换图片并保存。公开文章的封面会自动显示在首页和归档的左侧缩略图，以及文章详情页。移除封面后，列表恢复纸张占位。图片加载失败也会显示占位，不影响打开文章。

仅将图片插入正文或上传到附件库，不等于设置文章封面。主题不会抓取正文首图。会员和密码文章不在公开模板中输出封面地址，正文仍通过 Typlog 原生权限验证。`show_cover` 只控制详情页封面，不关闭列表缩略图。

操作参考：[Typlog 文章设置](https://docs.typlog.com/en/article/post-settings/)。本地 `/cover-demo/` 是专门标注的示例封面，并非你的真实文章配图。

## 首页额外配置：官方 PRO 机制

Typlog 的旧版主题接口提供 `query.config(name)`。官方文档注明这是 PRO 功能：主题读取名为 `_config/{name}` 的 JSON 资源，非 PRO 站点返回空值。

本主题已经接入 `_config/cali-typlog`。这是资源名称，**不是文章、页面路径或代码注入框**。若后台提供 JSON 资源管理，在那里创建或编辑该名称的资源，并放入 `config.example.json` 的内容。请通过后台「代码」管理 JSON 资源，不要把 JSON 粘贴到文章正文。

保存有效配置后，主题在服务端渲染时读取最新值，不需要改模板。若暂未生效，等待站点缓存刷新后再检查。

| 字段 | 用途 | 清空与默认行为 |
| --- | --- | --- |
| `intro` | 首页补充介绍；字符串数组，或按换行分段的字符串 | `[]` 清空；站点基础简介仍显示 |
| `portrait` | 顶部独立头像地址 | 默认 `asset:hero-portrait.png`；`""` 跟随 Logo；底栏始终跟随 Logo |
| `support_qr` | 打赏二维码 | 默认 `asset:support.png`；`""` 隐藏 |
| `wechat_qr` | 备用公众号二维码 | 默认 `asset:wechat-backup.png`；`""` 隐藏 |
| `recent_count` | 首页作品数量 | 默认 10；允许 1–30 的整数 |
| `recent_title` | 作品区标题 | 空字符串使用「写作」或英文默认标题 |
| `records` / `books` | 唱片和书籍收藏 | `[]` 隐藏对应分区 |
| `records_title` / `books_title` | 收藏分区标题 | 空字符串使用默认标题 |
| `projects` | 项目清单 | 默认显示三个产品；`[]` 隐藏项目页及其入口 |
| `projects_intro` | 项目页介绍 | 空字符串隐藏介绍 |
| `show_toc` | 文章目录 | `true` 显示；`false` 隐藏 |
| `show_cover` | 文章页封面 | `true` 显示；`false` 隐藏 |

可以只填写要修改的字段，其他值沿用主题默认配置。提供数组时会替换整个数组，不会自动追加。配置必须是有效 JSON，语法错误需要先在后台修正。已成功读取的配置如果字段类型错误，会使用默认值；无效收藏条目跳过，危险链接不会渲染。

### 首页介绍与订阅

站名在桌面使用 28 像素、手机使用 24 像素字号。站点简介采用柔和彩虹渐变，颜色随深浅模式调整；不支持渐变文字时显示普通文字。补充介绍仍可通过 `intro` 修改。

介绍末尾的「订阅更新」使用 Typlog 原生订阅弹窗；在后台关闭订阅功能时隐藏这一段。主题不会自动提交邮箱或发出订阅邮件。

顶部头像默认使用随主题附带的新图片生成黑白网点，左右和底部柔和淡出。手机上移到介绍文字上方。图片服务器不支持跨域采样或浏览器不支持画布时，保留柔边灰度原图；完全加载失败时显示站名首字。底部头像仍为彩色圆形，始终独立读取 Typlog 的 Logo；修改 `portrait` 不影响底栏。

### 首页三格入口与相册

首页三格入口始终使用「设置 → 链接与社交 → 主要链接」中的前三项。名称、地址、顺序和新标签页设置均随后台更新；不足三项时按实际数量显示。其余主要链接保留在完整菜单与页脚。没有主要链接时保留文章归档入口。

从 0.6.0 起，归档、棱镜通讯和隐说恢复小尺寸叠放相框，不再使用独立海报。站内归档和 `/tags/标签名/` 地址会读取目标列表中前三张不同的公开文章封面，包括 `/tags/Newsletter/` 和 `/tags/YINREAD/`。封面仍在文章编辑器中上传、替换或删除，栏目预览会随下次加载更新。只读取公开列表的封面标记，不抓取文章正文；会员、密码文章的封面不会进入预览。

从 0.4.0 起，旧的 `nav_cards` 配置不再覆盖后台主要链接。

将站内公开相册地址 `/moments/`，或某个已发布相册的地址（例如 `/moments/2023/0805/`）放入前三项，对应卡片会自动显示照片叠放预览。相册不必位于第二格，名称也可以自定义。卡片读取目标公开页面的前三张不同照片，每次打开页面重新获取；不会读取后台、草稿或私密内容。

预览请求不携带登录凭据，限定当前站点，禁止重定向，最多等待 6 秒并限制响应大小。空栏目、无封面、空相册、网络异常或关闭脚本时保留纸质相框和可点击入口，不显示虚构数量。Typlog 尚未提供本主题可用的相册总数接口，因此副标题使用「浏览相册」。

### 项目页与产品维护

安装后，底栏会自动增加「项目」。访问 `/#projects` 会显示独立的项目视图：识古、DockPic 和 portus 各有可点击的商店入口，portus 标明「美区等地区上架，中国大陆区暂未上架」。桌面使用紧凑三列清单，手机把简介放在产品名称下方。

无需另外建立 Typlog 页面。`/#projects` 是首页中的独立视图，支持直达、返回及前进；不是一个独立供搜索引擎索引的 `/projects` 路由。关闭脚本后仍能切换视图和打开产品链接。普通文章、相册和原有页面继续使用各自地址。

在 `_config/cali-typlog` 中填写 `projects` 即可修改产品、调序或新增产品。`title` 和 `url` 必填；`platform`、`description`、`notice` 和公开图标地址 `icon` 可省略。提供数组会替换完整清单。可从随包的 `config.example.json` 复制当前三个产品，再进行修改。不要仅为更新介绍而覆盖已有完整配置。

```json
{
  "projects_intro": "Tools for everyday ideas.",
  "projects": [
    {
      "title": "My app",
      "platform": "macOS",
      "description": "A small tool for daily work.",
      "notice": "Available in selected regions.",
      "url": "https://example.com/app",
      "icon": "https://example.com/icon.png"
    }
  ]
}
```

未设置或无法加载图标时显示产品名称首字，不影响跳转。默认识古图标引用官方 Chrome 商店图片；DockPic 和 portus 使用这次提供的图片，分别为 `asset:dockpic.png` 与 `asset:portus.png`，可在 `icon` 中更换。配置为 `"projects": []` 会同时隐藏项目视图和主题添加的入口。

### 联系、支持与二维码

从 0.7.0 起，页脚「关注」改为「联系」，原「索引」被移除。社交链接仍读取 Typlog 原生社交设置；名称、图标、细线分支与悬停预览组成紧凑清单。预览只展示链接目标，不额外请求社交平台数据。站内导航保留在底栏；偏好菜单仅保留外观和已启用的搜索。

右侧排列「打赏支持」和「微信公众号（备份）」两张二维码卡片。点击放大后可用关闭按钮、Escape 或点击弹窗外关闭；手机可打开原图后长按保存。不支持弹窗或关闭脚本时，卡片直接在新标签页打开原图。这里不会自动支付或订阅。

二维码使用提供的原图，只移除了 EXIF 与文字元数据，没有重新生成、裁切或修改码点；深色模式下仍保持白底。`support_qr`、`wechat_qr` 可替换为自己的公开图片地址，设置空字符串可以隐藏对应卡片。附带图片使用 `asset:` 标识，由主题自动解析到当前版本的静态资源路径；发布或手动复制主题时务必保留 `assets/` 文件夹。

### 添加书籍或唱片

当前默认配置已按提供的顺序收录 11 张专辑和 7 本书。数据保存在 `config.json` 的 `records` / `books` 中；后台同名字段可以替换整组内容，空数组隐藏对应区域。标题下的链接会在新标签页打开 Apple Music 或豆瓣。

`title` 必填，`author`、`cover`、`url` 可省略。已收录封面使用 `asset:` 标识并随主题打包；也可以使用公开图片地址。`width` / `height` 是原图尺寸，用于保持书封比例；`spine_color` / `spine_ink` 接受六位十六进制颜色，分别控制书脊底色和文字色。不填写时使用默认样式。

唱片可点击封套切换，也可横向拖动或使用触控板横向滑动，松手后吸附到最近的专辑；竖向手势保留页面滚动。书架点击书脊展开封面，其余书籍会倾斜让位；手机上整排按可用宽度等比例缩放。键盘左右方向键切换，Home / End 跳到首尾。音乐区域展示专辑收藏，不自动播放音频。

```json
{
  "books": [
    {
      "title": "Book title",
      "author": "Author name",
      "cover": "https://example.com/book-cover.jpg",
      "url": "https://example.com/book"
    }
  ]
}
```

省略封面会显示文字书封；省略链接则显示普通说明文字。图片和链接接受 HTTP(S) 或以单个 `/` 开头的站内地址；封面额外支持主题已打包文件的 `asset:` 标识。

## 没有 PRO 时

文章、页面、头像、基础简介和导航仍按上表正常编辑。额外首页字段无法通过该官方资源接口读取，主题会使用打包时的 `config.json`。

若需要修改这些额外字段，在源码中编辑 `config.json`，重新生成并安装三个模板。安装包附带的 `config.example.json` 是后台配置示例，修改它本身不会改变已安装的主题。不要把普通套餐描述成已支持专属主题设置面板：本项目没有新增 Typlog 后台表单的权限。

## 隐私

配置用于公开展示，请勿填写令牌、密码、订阅读者信息或私密图片地址。主题不读取后台账户、不请求文章正文副本，也不绕过会员或密码保护。

参考：[Typlog 模板与配置接口](https://docs.typlog.com/en/article/design-template/)、[基础设置](https://docs.typlog.com/en/topic/setup/)。

## 异常时的显示

- 头像加载失败时显示站名首字；文章缩略图显示纸张占位，书封显示书名。
- 脚本不可用时，收藏区保留普通文字与链接，避免留下无响应的切换按钮。
- 未开启搜索或订阅时隐藏对应入口。预览中的搜索只查询本地示例文章，邮件订阅明确停用；安装后的功能由 Typlog 处理。
- 缺少配置时仍保留默认肖像效果、近期 10 篇作品和原有导航。正文访问权限始终由 Typlog 原生组件控制。


## 联系卡片（本地待发布改动）

底栏按首页、归档、项目、后台次要链接、播客名片、关于、偏好排列。次要链接继续按后台顺序显示；长列表可横向滑动。偏好弹窗仅保留外观选项与可用的文章搜索，不重复列出导航链接。播客名片为固定入口，使用单色小宇宙图形，在新标签页打开。

页脚联系链接在鼠标悬停或键盘聚焦时展开名片。鼠标可以移入名片阅读，按 Escape 关闭。手机点击直接前往链接。已有社交链接和次要链接中识别到的社交平台会合并、去除相同地址。

默认卡片读取站名、后台 Logo、站点简介与链接中的账号名。没有实时调用平台接口，不会自动显示粉丝数或贡献数。可在 `_config/cali-typlog` 中添加 `contact_cards`，按完整链接地址或平台键覆盖：`x`、`telegram`、`github`、`zhihu`、`xiaohongshu`、`bilibili`、`youtube`、`wechat`、`instagram`、`sinaweibo`、`rss`、`email`。完整链接地址优先。

```json
{
  "contact_cards": {
    "x": {
      "name": "Your name",
      "bio": "Reading, life, and daily thoughts.",
      "avatar": "https://example.com/avatar.png"
    },
    "github": {
      "bio": "Projects and open source work."
    }
  }
}
```

`name`、`bio`、`avatar` 均可省略。不需要在公开配置中填写平台令牌。若自行提供已核实的数据，可添加 `stats` 数组，每项使用文字字段 `label`、`value`，最多显示三项；这是手动快照，不会自动刷新。图片加载失败时保留名字首字，未知平台仍保留正常链接。

预览地址 `/contact-demo/#projects` 使用示例社交账号与公众号链接，展示多平台卡片与导航排布；不会写入 Typlog 后台。


### 深色头像与外观

`portrait_dark` 指定深色模式的顶部头像，默认使用本次提供的打哈欠头像。它与 `portrait` 一样支持主题内资源或公开图片地址；留空时沿用浅色头像。导航栏头像仍使用 Typlog 后台站点头像。

浅色头像以深色点表现暗部；深色头像以浅色点表现亮部，并让边缘渐隐。无法读取像素或不支持 Canvas 时保留普通图片。

外观依次为「浅色」「跟随系统」「深色」。跟随系统会实时响应系统外观变化；手动选定浅色或深色后不再跟随。选择沿用 Typlog 的标签页会话周期，刷新和站内跳转后保留；不跨设备同步。
