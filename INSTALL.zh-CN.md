# 在 Typlog 使用 Cali 主题

这是一份完整主题，适配你当前 Ueno 0.8.0 使用的旧版主题接口。
它不是上一轮的 CSS 注入补丁，也不能直接运行原站的 React 代码。

## 文件对应关系

| 文件 | 用途 |
| --- | --- |
| `home.j2` | 首页：黑白网点肖像、三格入口、编号分区、近期 10 篇文章与底部导航 |
| `list.j2` | 归档、标签、作者与语言列表页 |
| `item.j2` | 文章、播客与独立页面 |
| `theme.json` | 主题名称、版本与仓库信息 |

三个模板都包含完整样式和脚本，只依赖 Typlog 原生宏与组件。
文章列表显示小尺寸封面；无封面时使用纸张纹理缩略图。
顶部黑白网点肖像默认使用随主题附带的新头像；底栏独立跟随 Typlog Logo，保留彩色圆形头像。

新版以 2026 年的 cali.so 为基准。默认介绍参考你的公开关于页。日常内容在后台编辑，额外配置优先读取官方 PRO 资源 `_config/cali-typlog`，没有该资源时使用源码中的 `config.json`。
具体字段、配置示例及普通套餐限制见 [内容维护指南](CONTENT.zh-CN.md)。
`records` 和 `books` 是可选的唱片、书籍列表；每项填写 `title`、`author`、`cover`、`url`，留空就不显示。
配置会编译进公开模板，不能填写令牌或私密资料。

## 当前发布状态

主题仓库已发布：[wangyr45/cali-typlog](https://github.com/wangyr45/cali-typlog)。主题版本标签需要与 `theme.json` 的版本一致。安装时请选择 `wangyr45/cali-typlog@0.8.4`，并检查首页、归档、关于、栏目及使用默认模板的文章。

0.8.4 包含唱片与书架、联系卡片、独立深色点阵头像和三档外观设置，已检查电脑与手机预览。

已经使用此仓库安装时，进入「设置 → 主题与设计 → 更换」，在自有主题输入框填写 `wangyr45/cali-typlog@0.8.4`，点击紧邻该输入框的更换按钮。此次发布不会自动替你切换线上主题。

可以直接从下方第 5 步开始。第 1–4 步供另建主题仓库时参考。

## 推荐路径：PRO 自有主题仓库

Typlog 官方支持在主题选择器中直接输入自己的 GitHub 主题仓库，不需要先被官方主题市场收录。这是 PRO 功能，仓库必须公开，并且需要与 `theme.json` 中 `version` 一致的 Git 标签。

操作依据：[Use Your Own Theme](https://typlog.com/changelog/use-own-theme)。请使用后台当前可见的自有主题入口。

1. 新建一个用于发布主题的公开 GitHub 仓库，例如 `wangyr45/cali-typlog`。本项目已使用该名称发布。
2. 解压 `dist/cali-typlog-0.8.4.zip`，将包内文件直接放在仓库根目录。根目录必须能看到 `theme.json`、`home.j2`、`list.j2`、`item.j2`；保留完整 `assets/` 图片目录、许可证和署名文件。
3. 把 `theme.json` 的 `repo` 改为实际仓库，例如 `wangyr45/cali-typlog`。当前包已填写 `wangyr45/cali-typlog`；仅在使用其他仓库时修改。
4. 提交并推送文件，然后创建和推送与 `version` 相同的标签 `0.8.4`。只上传文件，没有对应标签，也不满足官方要求。
5. 在后台进入「设置 → 主题与设计」，点击当前主题的「更换 / Change」，在主题选择器中输入该 GitHub 仓库。
6. 按后台提供的选项载入并启用主题。在启用前完成下方真实站点验证，并记录当前主题及版本，保留原有注入代码副本，以便恢复。

当前主题源码在原博客仓库的 `typlog-theme/` 子目录，不能把原始 Next.js 博客仓库当作已经发布的 Typlog 主题仓库。ZIP 是文件分发包，官方说明的安装入口是 GitHub 仓库，不是上传 ZIP。

这份主题使用完整 Jinja 模板，不能粘贴到「插入代码」的页首或页尾框。如果账户另有完整模板编辑权限，需要先确认支持分别编辑三个页面模板；本文不假定普通「代码」入口具有该功能。

## 没有 PRO 自有主题入口时

可按 [Typlog 官方主题仓库](https://github.com/typlog/themes) 的规则提交主题，等待维护方处理后再使用主题选择器安装。登记前需补充公开的主题效果截图和正确的仓库信息，不要使用后台截图。当前包的 `images` 为空，尚未完成登记准备。

也可以向 Typlog 确认当前账户是否有其他完整模板安装方式。仅能使用「插入代码」时，无法直接安装这份完整主题。

## 切换与后续维护

- 如果已安装早期 `taiyin-ui-v1` 样式补丁，切换时备份并移除该补丁，避免与新主题叠加。
- 首次测试请使用真实站点预览，检查文章、搜索、订阅、评论、播客和访问权限。本地示例通过不等于服务端验证通过。
- 主要链接的前三项用作首页入口；底栏读取首页、归档、项目、已配置的关于入口以及全部次要链接。新增底栏按钮时，在「链接与社交」的「次要链接」中添加并更新。
- 日常发布文章、修改封面和调整链接不需要重新发布主题。只有修改模板本身时才需要更新主题版本。
- 公开仓库、主题文件和版本标签已发布，安装后可通过主题选择器确认当前版本。

## 验证清单

- 首页：有封面、无封面、无文章时均可正常显示。
- 项目：底栏进入项目视图，三个商店链接、地区提示和返回功能正常。
- 导航：底部偏好菜单可展开，长导航不覆盖内容，归档与标签链接正确。
- 文章：中文长文、图片、表格、代码块、目录跳转均正常。
- 主题：明亮和深色模式下文字可读，搜索和订阅弹窗正常。
- 权限：未登录时，会员文章与密码文章继续显示 Typlog 的访问提示；成功验证后才显示正文。
- 评论和播客：评论开关与播放器仍按 Typlog 设置工作。
- 在启用前检查站名、头像、简介及链接中没有原站作者的身份信息。

本地测试只验证模板逻辑，不能替代 Typlog 的真实权限与组件验证。

## 官方实时预览

旧版官方工具是 `serve-theme`。如果需要用你的站点数据实时预览，请在自己的终端中输入仅有主题权限的令牌。不要把令牌发到聊天、写进模板、提交到 Git 或放到公开预览页面。

在解压后的主题根目录中执行：

```sh
read -r -s TOKEN
export TOKEN
read -r SITE
export SITE
serve-theme
```

关闭工具后清理当前终端中的变量：

```sh
unset TOKEN SITE
```

## 还原

重新选择原主题，或恢复备份的三个模板与原注入代码。
本主题不迁移或修改文章、读者名单及订阅数据。

## 旧文章仍显示另一种版式时

Typlog 的文章可单独指定 Lotus、Snow 等模板，这些设置会优先于站点主题。若需要跟随 Cali，在文章编辑器的模板设置中选择 Default。安装主题本身不会批量覆盖已有文章的独立模板选择。
